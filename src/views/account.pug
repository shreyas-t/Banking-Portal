include ./header.pug

include ./transactions.pug

if (account.unique_name == 'credit' && account.balance > 0)
    br
    a(href='/services/Payment') Make a Payment

br
a(href='/') Back to Accounts Summary

